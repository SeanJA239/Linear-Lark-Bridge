# ------------------------------------------------------
# 1. Rust 构建产物 (最重要！这部分通常很大且不需要上传)
# ------------------------------------------------------
target/

# ------------------------------------------------------
# 2. 环境变量与密钥 (绝对安全防线)
# 即使你现在还没创建 .env 文件，预先写上也更安全
# ------------------------------------------------------
.env
.env.*
!.env.example

# ------------------------------------------------------
# 3. 操作系统生成的垃圾文件
# ------------------------------------------------------
.DS_Store
Thumbs.db

# ------------------------------------------------------
# 4. IDE 配置文件 (共享的 .vscode 调试/任务配置入库，其余忽略)
# ------------------------------------------------------
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp

# ------------------------------------------------------
# 4b. Coding-agent 本地文件 (个人覆盖不入库)
# ------------------------------------------------------
CLAUDE.local.md
.claude/settings.local.json

# ------------------------------------------------------
# 5. 本地测试脚本 (视情况而定)
# 如果你的 test-webhook.sh 里包含了真实的 URL 或 Secret，
# 请取消下面这行的注释（去掉 # 号）
# ------------------------------------------------------
# test-webhook.sh

# ------------------------------------------------------
# 6. Zensical doc site build output
# ------------------------------------------------------
site/
.zensical-cache/__pycache__/

# ------------------------------------------------------
# 7. Console runtime data (SQLite event log, config writes)
# ------------------------------------------------------
/data/

# ------------------------------------------------------
# 8. direnv + devenv state (devenv.nix / devenv.yaml are tracked)
# ------------------------------------------------------
.direnv/
.envrc.local
.devenv/
.devenv.flake.nix
devenv.local.nix
